export function thumbLogoPath(slug: string): string {
  return `/logos/partners/thumb/${slug}.webp`;
}

export function colorLogoPath(slug: string): string {
  return `/logos/partners/color/${slug}.png`;
}
